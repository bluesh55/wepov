var React = require('react');

var AddReasonButton = React.createClass({
  render: function() {
    return (
      <div>
        <a href="#">근거 추가</a>
      </div>
    );
  }
});

module.exports = AddReasonButton;
